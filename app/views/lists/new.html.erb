<% provide(:title, "New List") %>
<div class="jumbotron">
	<%= form_for @list, url: list_path, html: {class: "form-signin"} do |f| %>
		<%= check_box_tag(:include, class: "checkbox inline") %>
		<%= f.text_field :name, placeholder: "Item name" %>
	<% end %>
</div>
$(document).ready(function () {
  addNewItemForm();
});

function addNewItemForm(){
	var categoryId = '<%= @category.id %>';
	$('#append' + categoryId).append("<%= j render 'shared/user_item_form', list: @list, category_id: @category.id %>");
}